export default function (data) {
        while(1==1){
                console.log(symbol,interval,close.tail(10))
                console.log("时间：",Date.now())
                sleep(Math.random() * 10);
        }

        //return close.tail(3)
}